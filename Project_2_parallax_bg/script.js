const canvas = document.getElementById('canvas1');// Selecciona el elemento del HTML con el id "canvas1" y lo asigna a la constante "canvas".
const ctx = canvas.getContext('2d');// Obtiene el contexto 2D del canvas seleccionado y lo asigna a la constante "ctx" (context).
const CANVAS_WIDTH = canvas.width = 800;// Establece el ancho del canvas en 800 píxeles y lo asigna a la constante "CANVAS_WIDTH".
const CANVAS_HEIGHT = canvas.height = 700;// Establece la altura del canvas en 700 píxeles y lo asigna a la constante "CANVAS_HEIGHT".